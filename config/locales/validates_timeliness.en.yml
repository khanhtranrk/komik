en:
  errors:
    messages:
      invalid_date: "is not a valid date"
      invalid_time: "is not a valid time"
      invalid_datetime: "is not a valid datetime"
      is_at: "must be at %{restriction}"
      before: "must be before %{restriction}"
      on_or_before: "must be on or before %{restriction}"
      after: "must be after %{restriction}"
      on_or_after: "must be on or after %{restriction}"
  validates_timeliness:
    error_value_formats:
      date: '%Y-%m-%d'
      time: '%H:%M:%S'
      datetime: '%Y-%m-%d %H:%M:%S'

vi:
  errors:
    messages:
      invalid_date: "không phải là ngày hợp lệ"
      invalid_time: "không phải là thời gian hợp lệ"
      invalid_datetime: "không phải là thời điểm hợp lệ"
      is_at: "phải là %{restriction}"
      before: "phải trước %{restriction}"
      on_or_before: "phải vào hoặc trước %{restriction}"
      after: "phải sau %{restriction}"
      on_or_after: "phải vào hoặc sau %{restriction}"
  validates_timeliness:
    error_value_formats:
      date: '%d-%m-%Y'
      time: '%H:%M:%S'
      datetime: '%d-%m-%Y %H:%M:%S'
